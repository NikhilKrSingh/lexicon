import { RetentionFormValidPipe } from './retention-form-valid.pipe';

describe('RetentionFormValidPipe', () => {
  it('create an instance', () => {
    const pipe = new RetentionFormValidPipe();
    expect(pipe).toBeTruthy();
  });
});
