import { GetMomentDateFormatPipe } from './get-moment-date-format.pipe';

describe('GetMomentDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMomentDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
